import { Request, Response, NextFunction } from "express";

// export const validatorMiddleware = {
//   routeSanity: (request: Request, reply: Response, next: NextFunction): void => {
//     return process.nextTick(next);
//   }
// };